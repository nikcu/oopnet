<Window x:Class="WpfApp.Windows.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="World Cup 2018 - Settings"
        Height="400" Width="500"
        MinHeight="350" MinWidth="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        Closing="Window_Closing">

    <Window.Resources>
        <Style TargetType="Label">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,5"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,5"/>
            <Setter Property="Padding" Value="8,5"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0"
                   Text="World Cup Settings"
                   FontSize="24"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,20"/>

        <!-- Settings Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel VerticalAlignment="Center" Margin="0,10">

                <!-- Language Selection -->
                <Label x:Name="labelLanguage" Content="Language"/>
                <ComboBox x:Name="comboBoxLanguage"
                          SelectionChanged="ComboBoxLanguage_SelectionChanged"/>

                <!-- Championship Selection -->
                <Label x:Name="labelChampionship" Content="Championship" Margin="0,15,0,5"/>
                <ComboBox x:Name="comboBoxChampionship"/>

                <!-- Display Mode -->
                <Label x:Name="labelDisplayMode" Content="Display Mode" Margin="0,15,0,5"/>
                <ComboBox x:Name="comboBoxDisplayMode"/>

                <!-- Resolution (shown only when windowed) -->
                <Label x:Name="labelResolution" Content="Resolution" Margin="0,15,0,5"/>
                <ComboBox x:Name="comboBoxResolution"/>

            </StackPanel>
        </ScrollViewer>

        <!-- Buttons -->
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Margin="0,20,0,0">
            <Button x:Name="buttonSave"
                    Content="Save &amp; Continue"
                    Click="ButtonSave_Click"
                    IsDefault="True"/>
            <Button x:Name="buttonCancel"
                    Content="Cancel"
                    Click="ButtonCancel_Click"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
