<Window x:Class="WpfApp.Windows.TeamInfoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Team Info"
        Height="400" Width="350"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="ToolWindow"
        Loaded="Window_Loaded"
        KeyDown="Window_KeyDown">

    <Window.Resources>
        <Style TargetType="TextBlock" x:Key="StatLabel">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#666"/>
        </Style>
        <Style TargetType="TextBlock" x:Key="StatValue">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
        </Style>
    </Window.Resources>

    <!-- Animation trigger -->
    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <!-- Fade in animation - 0.5 seconds -->
                    <DoubleAnimation
                        Storyboard.TargetProperty="Opacity"
                        From="0" To="1"
                        Duration="0:0:0.5"/>
                    <!-- Scale animation -->
                    <DoubleAnimation
                        Storyboard.TargetName="mainGrid"
                        Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                        From="0.8" To="1"
                        Duration="0:0:0.5">
                        <DoubleAnimation.EasingFunction>
                            <CubicEase EasingMode="EaseOut"/>
                        </DoubleAnimation.EasingFunction>
                    </DoubleAnimation>
                    <DoubleAnimation
                        Storyboard.TargetName="mainGrid"
                        Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                        From="0.8" To="1"
                        Duration="0:0:0.5">
                        <DoubleAnimation.EasingFunction>
                            <CubicEase EasingMode="EaseOut"/>
                        </DoubleAnimation.EasingFunction>
                    </DoubleAnimation>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Window.Triggers>

    <Grid x:Name="mainGrid" Margin="20">
        <Grid.RenderTransform>
            <ScaleTransform ScaleX="1" ScaleY="1" CenterX="155" CenterY="180"/>
        </Grid.RenderTransform>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Team Name & FIFA Code -->
        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,20">
            <TextBlock x:Name="textTeamName"
                       Text="Team Name"
                       FontSize="24"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"/>
            <TextBlock x:Name="textFifaCode"
                       Text="(FIFA)"
                       FontSize="16"
                       Foreground="#666"
                       HorizontalAlignment="Center"/>
        </StackPanel>

        <!-- Separator -->
        <Border Grid.Row="1" Height="2" Background="#ddd" Margin="0,0,0,15"/>

        <!-- Statistics -->
        <StackPanel Grid.Row="2">
            <TextBlock Text="Match Statistics"
                       FontSize="16"
                       FontWeight="SemiBold"
                       Margin="0,0,0,15"/>

            <!-- Matches Played -->
            <Grid Margin="0,5">
                <TextBlock Text="Matches Played" Style="{StaticResource StatLabel}"/>
                <TextBlock x:Name="textMatchesPlayed" Text="0" Style="{StaticResource StatValue}"/>
            </Grid>

            <!-- Wins -->
            <Grid Margin="0,5">
                <TextBlock Text="Wins" Style="{StaticResource StatLabel}"/>
                <TextBlock x:Name="textWins" Text="0" Style="{StaticResource StatValue}"/>
            </Grid>

            <!-- Draws -->
            <Grid Margin="0,5">
                <TextBlock Text="Draws" Style="{StaticResource StatLabel}"/>
                <TextBlock x:Name="textDraws" Text="0" Style="{StaticResource StatValue}"/>
            </Grid>

            <!-- Losses -->
            <Grid Margin="0,5">
                <TextBlock Text="Losses" Style="{StaticResource StatLabel}"/>
                <TextBlock x:Name="textLosses" Text="0" Style="{StaticResource StatValue}"/>
            </Grid>

            <Border Height="1" Background="#eee" Margin="0,10"/>

            <!-- Goals For -->
            <Grid Margin="0,5">
                <TextBlock Text="Goals For" Style="{StaticResource StatLabel}"/>
                <TextBlock x:Name="textGoalsFor" Text="0" Style="{StaticResource StatValue}"/>
            </Grid>

            <!-- Goals Against -->
            <Grid Margin="0,5">
                <TextBlock Text="Goals Against" Style="{StaticResource StatLabel}"/>
                <TextBlock x:Name="textGoalsAgainst" Text="0" Style="{StaticResource StatValue}"/>
            </Grid>

            <!-- Goal Difference -->
            <Grid Margin="0,5">
                <TextBlock Text="Goal Difference" Style="{StaticResource StatLabel}"/>
                <TextBlock x:Name="textGoalDifference" Text="0" Style="{StaticResource StatValue}"/>
            </Grid>
        </StackPanel>

        <!-- Close Button -->
        <Button Grid.Row="3"
                Content="Close"
                Padding="20,10"
                HorizontalAlignment="Center"
                Click="ButtonClose_Click"
                IsCancel="True"/>
    </Grid>
</Window>
